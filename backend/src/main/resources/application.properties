server.port=8080

spring.main.allow-bean-definition-overriding=true
spring.application.name=talent-platform-backend

# PostgreSQL Database Configuration (Railway & Local)
spring.datasource.url=jdbc:postgresql://${PGHOST:localhost}:${PGPORT:5432}/${PGDATABASE:Slack Hiring}
spring.datasource.username=${PGUSER:postgres}
spring.datasource.password=${PGPASSWORD:Karan@8251}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


# JWT Configurations
jwt.secret=9a4f2c8d3b7a1e5f8g9h0i1j2k3l4m5n6o7p8q9r0s1t2u3v4w5x6y7z8a9b0c1d
jwt.expiration=86400000

# Slack Bot and Client Integrations
slack.client.id=${SLACK_CLIENT_ID:11295333332627.11299721881446}
slack.client.secret=${SLACK_CLIENT_SECRET:mock-client-secret-removed-for-github}
slack.redirect.uri=${SLACK_REDIRECT_URI:http://localhost:8080/api/auth/slack/callback}
slack.bot.token=${SLACK_BOT_TOKEN:xoxb-mock-bot-token-removed-for-github}
slack.signing.secret=${SLACK_SIGNING_SECRET:mock-signing-secret-removed-for-github}
